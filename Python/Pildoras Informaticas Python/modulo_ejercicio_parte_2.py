#Ejemplo parte 2

from modulos import sumar 

#Esto es como javascript / el from localiza el archivo que queremos traer / y el import: importara la funcion que queramos traer a nuestro nuevo archivo.

#Pero si nosotros queremos traer todo el contenido de un archivo no lo vamos a llamar uno por uno, en este caso usaremos el * esto permitira que nos traera todas las funciones o clases que se encuentren dentro del archivo externo en este caso en el archivo modulo / ejemplo:

# from modulos import * 



sumar(2,3) # De esta manera podremos utilizar la funcion, clase como mas deseemos.




